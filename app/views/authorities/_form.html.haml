= simple_form_for @authority do |f|
  = f.input :name
  = f.input :basename
  = f.button :submit, value: t('.save'), class: "btn-primary"
  = link_to t('.back'), ( @authority.persisted? ? authority_path(@authority) : authorities_path  ), class: "btn btn-link"
